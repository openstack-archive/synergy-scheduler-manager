============
Installation
============

At the command line::

    $ pip install synergy-scheduler-manager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv synergy-scheduler-manager
    $ pip install synergy-scheduler-manager
